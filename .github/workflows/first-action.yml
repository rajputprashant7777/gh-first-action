name: first-workflow
on: workflow_run
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print-greetings
        run: echo "Hello World !!!"
      - name: print goodbye
        run: echo "Done -be!y!"
